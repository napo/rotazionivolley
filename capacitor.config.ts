import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'it.volleyserve.rotazioni',
  appName: 'Rotazioni Volley',
  webDir: 'web/dist',
};

export default config;
